/// <reference types="vite/client" />

// This one line teaches TypeScript about Vite's special imports — including
// `import text from "./file.mgf?raw"`, which we use in main.ts to load the
// spectrum file as a plain string.
